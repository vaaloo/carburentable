import {Text, View} from "react-native";

interface Props {
    props?: Station;
}

export default function StationItem(props: Props) {
    return (
        <View>
            <Text>LALALALLALAL</Text>
        </View>
    )
}